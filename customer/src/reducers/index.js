function returnReducer(state, action) {
    switch (action.type) {
        case 'SET_POPULAR_ITEMS':
            break;
        default:
            return state;
    }

}

export default returnReducer;
